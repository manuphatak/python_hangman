[run]
source = hangman

[report]
show_missing = true
exclude_lines =
    pragma: no cover
    # Don't complain if non-runnable code isn't run:
    if 0:
    if __name__ == .__main__.:
[html]
directory = coverage-report